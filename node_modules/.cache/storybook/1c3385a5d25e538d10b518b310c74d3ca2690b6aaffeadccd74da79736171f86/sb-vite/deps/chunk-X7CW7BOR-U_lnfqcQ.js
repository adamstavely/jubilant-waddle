import { t as require_preview_api } from "./preview-api-Fv5C2LpR.js";
import { t as require_global } from "./global-p-N7XMuC.js";
import { dedent } from "./ts-dedent.js";
import { _$LH, render as render$1 } from "./lit.js";
//#region node_modules/lit-html/development/directive-helpers.js
/**
* @license
* Copyright 2020 Google LLC
* SPDX-License-Identifier: BSD-3-Clause
*/
var { _ChildPart: ChildPart } = _$LH;
window.ShadyDOM?.inUse && window.ShadyDOM?.noPatch === true && window.ShadyDOM.wrap;
/**
* Tests if a value is a TemplateResult or a CompiledTemplateResult.
*/
var isTemplateResult = (value, type) => type === void 0 ? value?.["_$litType$"] !== void 0 : value?.["_$litType$"] === type;
//#endregion
//#region node_modules/@storybook/web-components/dist/chunk-X7CW7BOR.mjs
var import_preview_api = require_preview_api();
var import_global = require_global();
var __defProp = Object.defineProperty;
var __export = (target, all) => {
	for (var name in all) __defProp(target, name, {
		get: all[name],
		enumerable: !0
	});
};
var entry_preview_exports = {};
__export(entry_preview_exports, {
	parameters: () => parameters,
	render: () => render,
	renderToCanvas: () => renderToCanvas
});
var { Node } = import_global.global, render = (args, context) => {
	let { id, component } = context;
	if (!component) throw new Error(`Unable to render story ${id} as the component annotation is missing from the default export`);
	let element = document.createElement(component);
	return Object.entries(args).forEach(([key, val]) => {
		element[key] = val;
	}), element;
};
function renderToCanvas({ storyFn, kind, name, showMain, showError, forceRemount }, canvasElement) {
	let element = storyFn();
	if (showMain(), isTemplateResult(element)) {
		(forceRemount || !canvasElement.querySelector("[id=\"root-inner\"]")) && (canvasElement.innerHTML = "<div id=\"root-inner\"></div>");
		render$1(element, canvasElement.querySelector("[id=\"root-inner\"]")), (0, import_preview_api.simulatePageLoad)(canvasElement);
	} else if (typeof element == "string") canvasElement.innerHTML = element, (0, import_preview_api.simulatePageLoad)(canvasElement);
	else if (element instanceof Node) {
		if (canvasElement.firstChild === element && !forceRemount) return;
		canvasElement.innerHTML = "", canvasElement.appendChild(element), (0, import_preview_api.simulateDOMContentLoaded)();
	} else showError({
		title: `Expecting an HTML snippet or DOM node from the story: "${name}" of "${kind}".`,
		description: dedent`
        Did you forget to return the HTML snippet from the story?
        Use "() => <your snippet or node>" or when defining the story.
      `
	});
}
var parameters = { renderer: "web-components" };
//#endregion
export { renderToCanvas as i, parameters as n, render as r, entry_preview_exports as t };

//# sourceMappingURL=chunk-X7CW7BOR-U_lnfqcQ.js.map